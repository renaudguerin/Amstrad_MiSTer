`verilator_config

// Release 5.020 reports the initialized local Alt-key state and its
// nonblocking update as unsupported mixed scheduling.  P10 input coverage does
// not drive Alt, so this waiver is inert for the exercised keyboard path.
lint_off -rule BLKANDNBLK -file "*/rtl/hid.sv"
