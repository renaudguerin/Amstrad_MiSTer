# CRTC3 demo compatibility investigation

Base `18c2dce` (2026-09-25), Plus stream. The unchanged cartridge is
`local/test_media/cartridges/crtc3_v2fix.cpr`, SHA-256
`3d3f5e01c291e97c9284d10e27f12c0fda3e9530933f0919c607f354e38a7ad6`.
Private evidence is under `docs/references/crtc3-2026-09-25/` (this checkout's
`docs/references` resolves to `docs/specs`). No copyrighted media is committed.

## Reported symptoms and baseline

The user reports the early FlowLIB real-machine warning, a green top-right
streak around38s, left-edge lines around49s, displaced Buddha background
around72s, damaged dotted sphere around103s, background lines around108s,
and a crash around178s after two DMA guitar samples. Scene times are
navigation aids: the reference video and AmSpirit both show a transition at
exactly72s; the Buddha scene is present at75s and80s.

Baseline hardware is `Amstrad_20260923_edaa15b.rbf`, SHA-256
`d974447958841e5360151539ccc8a13cef6c0996fc74fffaf645adacca5beebf`.
No later synthesized-source change exists through the investigation base.
The capture driver pins this RBF and cartridge, with explicit6128+, Full sync,
and Original CPU timing. `hardware-baseline-02/boot/` reproduces the warning;
`hardware-baseline-03/progression/` records ten serial native captures, including
left-edge streaks and displaced Buddha bands. Its last torn-twister capture
remains byte-identical in three additional captures several minutes later
(`hardware-continuation/`, hash
`e9c910f0472d4e361700c7fe172e061b3bcc55a5f9e19b38f2029e3fd424af09`).
This supports a frozen-output failure, rather than a single corrupt animation
frame; a scaler image alone does not identify whether CPU or video stopped.
These are MiSTer scaler captures,
not original CPC hardware evidence. Settings were restored byte-for-byte:
CFG SHA-256 `2e585b4c85e2387cfb9c25028a061c6ba2aa3749f82453ffd895b5aaa393d8e4`.

AmSpirit Lite1.15.1/core2491682, model4/CRTC3 supplies reference screenshots,
full snapshots and API state under `amspirit/sec-*/`. The emulator passes the
FlowLIB check and shows coherent Buddha/sphere backgrounds. Its original
snapshot, configuration, rendering and pause state were restored after each
collection. `amspirit/sphere-phase.json` records debugger pre-instruction
positions; these must not be equated to CPU bus sampling or raw CRTC C0.

## FlowLIB warning: established trigger

Unchanged-cartridge production-T80 replay enters the machine check at&37B7.
The first stage executes repeated LD(DE),A at&9000 (A13=0), expecting a
non-raster IM2 vector. The core takes82 raster handlers and no alternate
handler; BF01 remains0. At&37ED it returns A=1, and caller&0119 paints the
warning. AmSpirit's1s snapshot contains the third-stage HALT routine at&9000,
showing that it passed the first-stage check.

The source of the deliberate hardware bug is external Plus bus logic:

```
ASIC_IORQ_n = CPU_IORQ_n | (~physical_READY & ~A13) | reset
```

The [CPU schematic](https://oldwiki.cpcwiki.eu/imgs/8/84/CPC_Plus_CPU_Schematic.jpg)
connects IC116G3 to CPU /WAIT (the raw ASIC READY net) and A13. The diode/reset
network and NOR stages yield the equation above. The
[original circuit discussion](https://oldwiki.cpcwiki.eu/forum/amstrad-cpc-hardware/amstrad-plus-and-im2-bug/100/)
and [measured trace discussion](https://oldwiki.cpcwiki.eu/forum/amstrad-cpc-hardware/amstrad-plus-and-im2-bug/150/)
confirm the mechanism; the [logic-analyser image](https://oldwiki.cpcwiki.eu/imgs/7/7e/IM2_Plus_Ack_Bug.png)
shows one CPU acknowledge becoming two ASIC-side pulses, vector06 then04.

Use raw Plus READY, not synthetic cartridge/DMA stalls or the T80 idle-WAIT
workaround. The circuit also affects ordinary I/O. Instruction sensitivity
must emerge from bus phase; an A13-only vector override is not this mechanism.
The original machine check's second-stage RET NZ does not set flags from its
newly loaded marker, so a regression test must use an explicit flag-setting
check rather than copying that weakness.

Private `flowlib/README.md` records disassembly, original source captures,
complete replay commands and the implementation handoff.


## Acknowledge repair: simulation evidence

The motherboard now separates raw expansion IORQ from the shaped ASIC-side
signal. ASIC peripherals and ASIC-derived FDC selects consume the latter;
expansion peripherals retain the former. The vector latch remains stable for
each pulse. An empty repeated pulse within the same M1 returns04, while an
isolated empty acknowledge retains00. Low-A13 FDC alias timing is schematic
inference rather than a directly measured acceptance claim.

The production-T80 diagnostic failed before repair with one ASIC pulse and
vector06. Board shaping alone produced06,00; the completed repair produces
06,04 and reaches the alternate handler. High-A13 write and low-A13 HALT
controls retain a single06 pulse. DMA and raster provenance checks pass.
The focused command is `make -C sim/plus b20-bus-diag b20-ack-diag`, using
GHDL6 and LLVM clang++; its accepted log is `ack-fix/accepted-focused.log`.
ASIC register unit vectors also pass. The unchanged cartridge replay completes
all three FlowLIB checks and branches from0117 to0130, skipping warning0119.
The final selected gate passes40 benches; cross-provider review and synthesized
hardware acceptance are pending.

## Cross-line raster interrupt: second finding

[Revised Arnold §2.4](https://web.archive.org/web/20250102091704/https://www.cpcwiki.eu/index.php/Arnold_V_Specs_Revised)
describes an additional interrupt at entry to a matching scanline when raw
HSYNC overlaps that boundary. The existing implementation only handles the
ordinary monitor-HSYNC trailing edge. A connected production GA/video probe
with R0=63, R2=51 and HSYNC width14 fails: it produces the ordinary event but
omits the line-entry event. R2=49 is a noncrossing control and retains exactly
the ordinary event. A candidate supplying the missing event passes both.

Settled snapshot A/B replays reproduce the corrupted Buddha background and
fragmented sphere with baseline RTL, and coherent images with the candidate.
The Buddha SPLT1 write moves from line2 to line1, before the existing split
capture. No screen-split comparator change is needed. Snapshot convergence
is supporting diagnostic evidence: omitted transient state and an explicitly
reconstructed RMR2 prevent treating it as an exact normal-boot timeline.

The sphere uses R2=50, where raw HSYNC falls on the line boundary. Using the
previous raw-HSYNC sample repairs this scene, but that simultaneous-edge
ordering remains an inference requiring hardware adjudication. The source
clearly establishes the R2=51 overlap case, not the exact R2=50 seam.
Focused commands, before/after logs and replay limits are in the private
`late/PRI_FINDING.md`. Production implementation passes the connected regression, reset/import/live-write
guards and eight GA differential tests. The existing synthetic PRI fixture had
reversed HSYNC polarity; corrected active-high stimulus with a full monitor
shaper window passes its unchanged assertions on both old and new RTL.
Hardware acceptance remains pending.

## Eerie Forest left edge: bounded exclusions

The production replay shows no missed video fetch or stale SDRAM return at
the reported SSCR change. Six synthetic two-colour raster probes agree with
AmSpirit across all624×200 active pixels after geometry alignment. These
include writes at C0=1,2,3 with12-dot delay; the C0=2 zero-delay control moves
by exactly12 dots when scrolling is enabled. This excludes a general early-line
serializer/scroll/mask phase defect under those conditions. Original hardware
bus phase and title-specific interrupt/software timing remain open. Private
`edge/README.md` records the programs, traces and comparison method.


## Validation and remaining acceptance

Two production repairs are implemented. Native Astra-medium review found no
blocking issue in either; it noted that the B20 sample probe observes the
upstream CPU bus, while the distinct handler marker independently proves
consumption. Replaced LDIR cases narrow instruction-class coverage. Gemini
OAuth retries timed out; Opus review exited by provider signal9 without output.
Muse review is running after the user resumed the goal. Cross-provider review
debt remains until that review completes.

All diagnostic replay processes are stopped. The late twister uses R2=42,
width14, so the new cross-line event is inactive there. A corrected snapshot
replay progressed for three simulated seconds with identical baseline/candidate
frame50 hashes; it did not reproduce the freeze. Earlier DMA/history remains
unresolved. Scene-specific mapper state must be derived from code: address0061
is a mapper shadow in the sphere handler but an opcode in the twister handler.
Private `late/TWISTER_STATUS.md` contains the restart commands and limitations.

The selected gate passed with
`MAKEFLAGS='CXX=/opt/homebrew/opt/llvm/bin/clang++' GHDL=/tmp/eerie-ghdl/ghdl-llvm-6.0.0-macos15-aarch64/bin/ghdl python3 sim/select_tests.py --run`: `select_tests: PASS 40 benches`.
The initial attempt selected the system compiler despite an environment-only CXX
override and failed on missing C++ headers; the explicit Make override resolves
that host-toolchain issue. Logs are preserved in private `selected-gate-llvm.log`.

Next acceptance steps: obtain the cross-provider review, synthesize this exact source, and replay the unchanged CPR on
MiSTer through at least200 seconds. Compare FlowLIB boot, Buddha, sphere,
Wolverine and both edge streaks; retain explicit test of the provisional R2=50
seam. Investigate the late freeze separately if it persists. Eerie's remaining
sliver is not explained by this PRI fix; the next bounded discriminator is the
flat-plane scroll probe driven through a minimal ordinary PRI handler.
No new RBF, integration, push or hardware acceptance is claimed at this checkpoint.
