# B6/B10 independent source review — 2026-09-02

Guarded Claude Opus review of committed `a98590a33345f2c529fcd6d30676235d4193bfd7`.
Verdict: **B6 conditional menu visibility CLEAR; B10 ROM-loader routing extraction CLEAR.**
No functional defect in either. Source-only review: no gates run, no HPS-path
simulation, OSD rendering unprovable from this repository. Follow-up suggestions
below remain pending triage against repository test policy; they are not
instructions to add source-scraping tests or perform cosmetic refactors.

Base: `a98590a` (committed tip, not concurrent FDC work). Working-tree FDC diff,
`rom_map`-without-reset (B13, carved out), other debt rows, and all
hardware/RBF claims were out of scope and untouched.

## B6 — verified CLEAR

- Bit ordering correct: `rtl/plus/plus_menu_capability_mask.v:15-21` yields,
  LSB-first, `[0]=0`, `[1]=en270p`, `[2]=plus_mode`, `[3]=!plus_mode`,
  `[4]=!plus_mode||has_fdc`, `[5]=!plus_mode||has_tape`. Every CONF_STR prefix
  matches its bit (`d2F8,CPR`; `d3P1O2,CRTC` + `d3P2O[5:4],Model`;
  `d4S0`/`d4S1`/`d4P2OGH,FDC`; `d5F4,CDT`/`d5OK,Tape sound`). Width 16 matches
  `sys/hps_io.sv:122`. No collision with crop bit 1.
- Capability table right: `rtl/plus/plus_model_select.v:29-46` — GX4000 neither
  medium, 6128+ FDC only, 464+ tape only, `2'b00`/unknown fail closed; classic
  keeps DSK, FDC, CDT and tape sound. Fixture `0x38/0x04/0x14/0x24` (`+0x02`
  with crop) in `sim/plus/b6_menu_mask_test.cpp:20-25` matches the reviewer's
  independent derivation — rule-derived, not read back from the simulator.
- Encoded settings preserved: the `ff9fa18` CONF_STR diff adds prefixes only; no
  `O`/`R` index renumbered. Every masked entry carries an explicit slot digit
  (`S0`, `S1`, `F4`, `F8`, `OK`, `O2`, `OGH`, `O[5:4]`), so masking cannot
  renumber `ioctl_index` or the VDNUM-2 disk slots.
- Detach ownership clean, CPR image survives `R[32]`: `status[32]` appears only
  at `Amstrad.sv:726`/`:733` (`reset_base`) and `:1396`
  (`.dandanator_detach`); the cartridge service takes `.detach(1'b0)` (`:1238`)
  and `.cold_reset(reset_base)` (`:1234`); `rtl/plus/plus_cartridge_memory.v:179-193`
  omits `image_valid` from the `cold_reset` arm while `detach_event`
  (`:147-148`) and `abort_event` (`:163-164`) clear it.

### B6 follow-ups (low, non-blocking)

- **B6-1 (user-facing).** `Amstrad.sv:103` `R[32],Reset & Detach Cartridge` is
  mislabeled and the one media control with no capability prefix: after
  `a27fdb1` it detaches nothing on a Plus machine yet keeps a cartridge label
  visible in every mode. `docs/b6-architecture-decision.md:84-86` defers
  Dandanator scoping deliberately, so this is a consistency gap, not a defect.
  Rename, or add a Dandanator-presence bit.
- **B6-2 (UX, undocumented).** `Amstrad.sv:673` — the mask follows raw
  `status[34:33]`, so classic media controls vanish before the reset that
  applies the model. Consistent with decision 3 (reachability only) but the
  mirror image of decision 2's raw-`plus_mode` reasoning, and written down
  nowhere. One sentence in `docs/b6-architecture-decision.md` closes it.

## B10 — equivalence against the pre-extraction source CLEAR

Clause-by-clause against `44eb1b7^:Amstrad.sv`; every one matches:

- Qualification, bank, promotion: `rom_active` (`:19`),
  `(idx==7)?2'd2:{1'b0,&idx[7:6]}` (`:36`), `promote` (`:23`) verbatim;
  promotion gating at `Amstrad.sv:400` unchanged.
- The `dest_a_hi` concat does not leak carry: `{page[8], page[7:0] +
  ioctl_addr[21:14]}` (`:37`) — the add is a self-determined 8-bit operand
  inside the concatenation, truncating exactly as `boot_a[21:14] <= page[7:0] +
  ioctl_addr[21:14]` did. Bit 8 stays `page[8]`.
- Invalid index-zero chunks still retain state: old code hit `default:
  romdl_wait <= 0;` in the first case with no default in the bank case, so both
  registers retained. New code returns `addr_valid=0`, and the wrapper
  (`Amstrad.sv:387-391`) writes neither register and clears `romdl_wait`.
  Equivalent.
- Branch-guard change safe: `else if(ioctl_index)`/`else` → `else if
  (rom_download)` is equivalent because the outer condition only reaches the
  third arm once the SNA/Dandanator arms are taken.
- `page`/`combo` promotion to module scope behavior-neutral (`Amstrad.sv:323-324`):
  the combinational net settles on the pre-edge `page` value, matching the old
  same-block nonblocking read; `:345` is the only writer.
- Fixture genuinely independent and both binaries run:
  `legacy_route` (`sim/plus/rom_loader_route_test.cpp:36-61`) restates the old
  RTL in C++; exhaustive sweep covers 256 indices x 2048 chunks x 6 pages.
  `files.qip:24`/`:35` register both modules; `sim/plus/Makefile:423+`
  executes both test binaries in the default target — the earlier "built but
  not run" defect is closed.

### B10 follow-ups (non-blocking; do not reopen B10)

- **B10-1 (medium, worth acting on).** `Amstrad.sv:386-392` — nothing pins the
  wrapper side of the invalid-chunk contract: `boot_bank`/`boot_a[22:14]` go
  unwritten, but the fixture proves only `addr_valid=0` at the module boundary.
  Dropping the `if (rom_route_valid)` guard would silently break retention with
  the suite green. Precedent for closing: source-scraping pattern at
  `sim/plus/sdram_cartridge_test.cpp:780-793`.
- **B10-2 (low).** `Amstrad.sv:332` instance shadows the module name
  (`rom_loader_route rom_loader_route`). Legal and accepted by Quartus 17.0.2
  (run `33482930072`), but defeats hierarchy-path greps. Rename to `rom_route`.
- **B10-3 (low).** `Amstrad.sv:323-324` — `page`/`combo` lost their structural
  single-owner guarantee when moved out of the always block. Correct today;
  one-line ownership comment is cheap insurance.

## Evidence limits (retained, not review debt)

- `d<n>` rendering unprovable here: `sys/hps_io.sv` transports the mask; the
  prefix parser lives in MiSTer_Main `menu.cpp`, not vendored. Polarity is
  solid (pre-existing `d1P1OR,Vertical Crop` / `{en270p,1'b0}` pairing); what
  cannot be checked is hide-vs-grey and `S`/`F`-row versus `O`-row behavior.
  Actual OSD rendering is hardware/UI validation, not review closure.
- No HPS path simulated: `b6_menu_mask_test_top.v` exercises the two leaf
  modules only; the `Amstrad.sv:216` connection is read-verified.
- No gates run (read-only mandate); B10 equivalence is source-derived, not
  observed.

Published ACCC v1.11 unchanged; no corrected complete edition exists.
